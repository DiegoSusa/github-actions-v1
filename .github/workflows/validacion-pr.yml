name: Validacion de pr

on: 
  pull_request:
    types: [opened, synchronize]

jobs:
  validacion:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar el repositorio
        run: actions/checkout@v4

      - name: Saludar al actor
        run: echo "Hola actor ${{ github.action }}, todo funciona de maravilla"

      - name: Verificacion
        run: echo "Verificando que todo esté en orden..."
